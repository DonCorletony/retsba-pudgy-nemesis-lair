# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling / temp (RETSBA migration)
.claude/
_art_manifest.txt

# Art source library — only the web-optimized files ship; the ~280 source
# images (incl. the X posts folder) stay out of the repo and the deploy.
public/art/*
!public/art/door-*.png
!public/art/feature-*.png
!public/art/cut-*.png
!public/art/scene-*.png

# Ad source originals — keep only the optimized web JPEGs (desktop + mobile).
public/ads/*
!public/ads/ad-1.jpg
!public/ads/summer-2026.jpg
!public/ads/ad-1-mobile.jpg
!public/ads/summer-2026-mobile.jpg

# Logo source (un-optimized) — only the web logo ships.
public/logos/the retsba shop.png

# Merch source folders/originals — keep only the optimized gallery.
public/merch/*
!public/merch/gallery
!public/merch/gallery/**
